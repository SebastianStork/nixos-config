name: Build
description: Build a flake output
inputs:
  flake-output:
    description: Flake output to build
    required: true
runs:
  using: composite
  steps:
    - name: Build
      shell: bash
      run: nix build "${{ inputs.flake-output }}" --print-build-logs
